#include <stdio.h>

int main() {
  // Create an integer variable that will store the number we get from the user


 
 
// int speed=90;
// printf("%p", &speed);


int c=8;
int* ptr = &c;
printf("%p", ptr);
  return 0;
};