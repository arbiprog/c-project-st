#include <stdio.h>

int main() {
  // Create an integer variable that will store the number we get from the user

 int num;
 
 printf("what is the speed that you want drive with it ? \n");
 int speed;
    scanf("%i", &speed);
 printf("your speed is %i km/h", speed);
 
 
 
  return 0;
};